# React-Todo-Application

To run the todo application :
1 first we need to change the directory using cd student-mang-app
2 then u need to run the command npm run dev
3 and then finally click on the link u displayed on the editor screen
